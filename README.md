# CLA signatures storage

This branch stores Contributor License Agreement signatures for Ktesio,
recorded automatically by the CLA workflow (`.github/workflows/cla.yml` on `main`).

Do not edit `signatures/version1/cla.json` by hand — the CLA Assistant action
manages it. See `CLA.md` on `main` for the agreement contributors sign.
