---
epic: 5
date: 2026-09-10
verdict: accepted-with-open-items
criteria: declared
headless: false
---

# Epic 5 Retrospective — Wire Memory Consistently

## Epic summary

- **Epic:** 5 — Wire Memory Consistently (stories 5-1, 5-2; both `done`; `pending_stories: []` per `sprint_status.py detect-epic --epic 5`).
- **Diff range:** `ff1f669^..e0e9f6c` (5-1 "attach a managed filesystem Memory Backing" PR #138 → 5-2 "delegate to native memory with an explicit boundary" PR #139). 28 files, ~3,654 insertions. Retro DEFERRED from 2026-08-27 (epic closed on the all-stories-done rule; retro stayed optional/owed) until 2026-09-10 — epics 6–10 landed in between, so later-epic effects on the memory surface are in scope.
- **Evidence inventory:** sprint-status story entries (detailed), both story specs, deferred-work.md 5-1/5-2 sections, the range diff, current tree at `ba524c5`. Session logs for the epic itself are absent; the AI-63b lock-model decision document (2026-08-27) covers the lock questions the stories raised.

## Findings

### A. Cross-epic verification (the deferred retro's main value)

- **A1 [fix-now, high — NEW finding]:** the operator-spoofed `memory.dir` strip is PARTIAL. `start_inner` strips a hand-set `memory.dir` from the local effective config only (`supervisor.rs:787-800`); when invocation overrides exist (engine-observed instance → `base_url` set), the config re-folds from persisted layers UNSTRIPPED — so an operator-set `memory.dir` reaches `apply_config_mapping` and delivers through a declared mapping (hermes → `HERMES_HOME`) with no DC-10 notice. No test pins the strip at any layer (5-1's review record: E4 was a code-only patch). Docs at landing claimed "any hand-set value is stripped where it matters."
- **A2 [resolved]:** DC-10 stderr notice was pinned only through the `kt` binary at landing — resolved by 10-2 (`b590dc8`): `emit_diagnostic` choke point + `diagnostic_sink.rs:122-133` byte-exact pin.
- **A3 [resolved]:** `declared` delivery-fact invisibility on the attach surface — resolved by 6-6's `memory attach --json` (`agent.rs:1809-1857`).
- **A4 [fix-now, low]:** strict UTF-8 refusal (`supervisor.rs:826`) has NO test at any layer; Windows symlink refusal untested (unix-only test). 
- **A5 [record-corrected]:** the 5-2 deferred entry mis-states the untested conflict direction — the CLI reverse direction was tested in 5-1; the REGISTRY-level reverse is the genuinely untested one (then and now, `registry.rs:2998` forward-only).
- **A6 [record-corrected]:** the TOCTOU deferred entry's premise is overstated for in-process callers — `Engine::start` holds the registry mutex across the backing read + spawn, and `attach_memory` holds the same mutex; in-process they are mutually exclusive (the AI-63b decision doc §4.1 agrees). The real residual window is CROSS-PROCESS (embedded engine + separate `kt` share only SQLite), which the entry never named.

### B. Aggregate views

- **B1 [accept]:** spec-to-implementation reconciliation — every 5-1/5-2 sprint-status claim verified in the range (SCHEMA_V5, invocation-override seam, DC-10, terminal guard, symlink/UTF-8 refusals, native metadata-only, portability proof). One nuance: "BOTH directions" is CLI-true/registry-false (A5).
- **B2 [fix-now, med — carried]:** the deferred cost materialized — 6-6's contract_version bump touched 15 test files because memory.rs kept private builders. 10-1 landed the shared home (`test_support.rs`), but `memory.rs`'s 13 private writers were outside its scope and remain. Folds into open issue #164.
- **B3 [defer]:** migration crash-atomicity (batch-then-stamp, no `BEGIN IMMEDIATE`, DDL lacks `IF NOT EXISTS`) — unchanged since landing; no incident in 6–10. The `migrate()` comment ("re-runs only the steps it still needs") contradicts the finding (non-idempotent DDL re-runs would fail on `CREATE TABLE`). Needs a one-hour verify-and-harden, not a retro re-litigation.
- **B4 [defer]:** store-vs-registry idempotence split (REPLACE in store, preserve in registry) — unchanged; invariant lives only in registry.rs:612-663.
- **B5 [record]:** memory-path architecture deltas since landing are clean: 6-2 added the first real consumer (hermes `memory.dir` → `HERMES_HOME`, which surfaced the native-adapter/native-backing term collision — fixed in docs), 6-4 added the TCK memory section, 6-6 froze the wire, 10-2 routed the notice. The two-mutex lock model is unchanged; AD-18 (fine-grained lock proposal) remains unratified and unimplemented with `status: awaiting-ratification` — and epic 7 + 7-5's perf measurement effectively passed the gate AD-17 set for deciding it.

### C. Process observations

- **C1 [process]:** the retro deferral cost exactly what AI-66-style reasoning predicts — the deferred entries drifted (F5's direction mis-statement; F6's overstated premise) and the shared-test-support prediction ("the cost will materialize") came true in 6-6's 15-file bump. Owed retros should run at the NEXT epic boundary, not "eventually."
- **C2 [accept]:** the stories' own review records were honest about what was untested (E4 code-only, the end-to-end disclaimer in memory.rs:512-515) — which is what made this retro's re-verification tractable.

## Behavior verification

- Current-tree memory suite: `cargo test -p ktesio-engine --test memory` — 10/10 green (analyst re-ran).
- v0.7.0-era behavior verified in the epic-7 tests: memory attach/detach --json documents, hermes subject-delivery (`HERMES_HOME` exact match), self-heal.
- Not re-run at the landing commits: historical gate figures (~960/974 tests, 95.45%/95.47%) — taken from the story records.
- F1's engine-observed branch traced by code reading only; not reproduced with a live engine-observed instance.

## Previous-retro follow-through

Epic 4's retro (the prior completed-epic retro): AI-63 accepted-tradeoff (closed this week, epic-10 era), AI-65 adopted as standing rule, AI-66 subsumed, AI-69 ratified, AI-68 closed — all recorded on issue #168. The 5-1/5-2-era items AI-63(b) (lock model) and the shared test-support utility: see B2/B3/A6 — the utility home exists (10-1), the lock model awaits ratification with perf evidence in hand (7-5).

## Action items

1. **[high] memory.dir strip gap (A1):** strip operator-set `memory.dir` in the invocation-override branch too (or refuse the set), + a test at the engine-observed layer, + fix the stale docs claim. Owner: next engine-touching story.
2. **[low] Untested refusals (A4):** strict-UTF-8 test; Windows symlink-refusal test (or documented per-OS honesty). Owner: next test-touching story (folds into #164's consolidation).
3. **[med] Migration crash-atomicity verify-and-harden (B3):** confirm batch-then-stamp behavior, align the comment, add `BEGIN IMMEDIATE` or idempotent DDL. Owner: next store-touching story.
4. **[low] Registry reverse-conflict test (A5) + correct the deferred-work direction note.** Owner: next engine-touching story.
5. **[record] AD-18 lock-model ratification (B5):** 7-5's perf evidence effectively passes the gate; ask Islam to ratify the coarse model (retiring AD-18) or commission the split. Owner: Islam.
6. **[record] Shared test-support adoption for memory.rs + the 13 legacy writers (B2):** folds into open issue #164.

## Acceptance verdict

**accepted-with-open-items** (criteria: **declared** — epics.md stories 5.1/5.2 ACs + the ratified Q-1/Q-2 rulings). Both stories shipped their declared surfaces, verified line-by-line in the range diff; the wire freeze and --json surface landed in 6-6 as planned. The open items are hardening (A1's strip gap is the one real correctness finding — it narrows the DC-10 honesty guarantee to the non-observed path) and the carried deferrals.

## Open questions

1. **AD-18 ratification (needs Islam):** the fine-grained lock proposal has been unratified since 08-27; 7-5's perf evidence (0.7%/instance vs 2% budget) supports ratifying the coarse model. Ratify or commission the split?
2. **memory.dir semantics (needs Islam eventually):** should an operator hand-set `memory.dir` on an engine-observed instance deliver (current behavior in the override branch) or be refused with the DC-10-style honesty notice? A1 assumes refuse-and-notice is intended.
