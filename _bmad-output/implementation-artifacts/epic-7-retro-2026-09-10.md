---
epic: 7
date: 2026-09-10
verdict: accepted
criteria: declared
headless: false
---

# Epic 7 Retrospective — Embed the Engine (Hosts)

## Epic summary

- **Epic:** 7 — Embed the Engine (Hosts) (stories 7-1…7-5, all `done`; `pending_stories: []` per detect-epic).
- **Diff range:** `999164c..b590dc8` (branch feat/epic-10-consolidate-harden carried the epic's stories on top of the epic-10-adjacent fixes; the review diff and per-story review patches are in `_bmad-output/implementation-artifacts/epic-7-review-diff.patch` era records). Merged as PR #180 (squash b590dc8), 2026-09-10. 5 commits + epic-level review round.
- **Stories:** 7-1 UJ-3 library host + shared uj3 assertions (FR-31); 7-2 AD-14 event bus (FR-33); 7-3 embed-clean audits (FR-34, AD-13); 7-4 publish preparation — PREPARE-ONLY (FR-32); 7-5 NFR-4 budgets measured.
- **Evidence inventory:** per-story specs (spec-7-1..7-5), per-story review patches (story-7-x era, gitignored), the EPIC-level review diff (epic-10-review-diff.patch era covered epic-7's stories too — note: epic-7's stories were reviewed at story level with 13/17/20/15/17 patch findings respectively), the v0.7.0 release (live: 4 platform binaries, ktesio CLI 0.7.0 on crates.io), the three library crates published (0.1.0 each), docs site current, session logs present in-conversation for all five stories.
- **Note on publication timing:** story 7-4's crates.io publication was HELD at merge time per Islam's standing no-deployment instruction; the go was granted and steps 0–7 of the runbook executed on 2026-09-09/10 (release-tracking issue #176) — so by retro time, the AC's publication criterion is MET (all three lib crates + ktesio CLI live; Homebrew tap updated via the documented manual fallback after the workflow's token-auth failure).

## Findings

- **F1 [process, the headline]:** the epic-level triple review (run in parallel before the PR per Islam's standing instruction) earned its mandate: **82 per-story review findings** (13/17/20/15/17 across the five stories) plus **21 epic-level cross-story findings**. The cross-story round caught what no story review could: the NFR-4 fixture premise (25/10 consts) pinned only by a comment, the resync-then-subscribe ordering race stated as an absolute contract, the event bus unannounced in CHANGELOG, README roadmap staleness, and the planning-record drift (7-4's held publication unreconciled in epics.md). All applied.
- **F2 [process, real defect found and fixed mid-epic]:** the CF Pages docs deploy had been silently failing since ~Aug 26 ("No deployment available" on every push) because no Node version was pinned and the CF builder image drifted — four docs pages were 404 on the live site while CI stayed green (docs builds are not in CI). Fixed: `.node-version` at root + docs (5497ea7 → b8c2efc after the first pin attempt hung the builder; Node 22 un-pinned after the builder's auto-detected 22.16.0 proved correct — pins removed, builder default kept). Lesson recorded: **a docs deploy that nothing verifies will silently rot; the live site needs a probe in CI or a deploy-completion check** (deferred — see action items).
- **F3 [process, two CI failures during the cycle, both fixed]:** (1) the engine semver baseline pointed at a BRANCH commit (8a8b328) that the squash merge never put on main — the cat-file guard reds every post-merge run (fixed: baseline → 12f3aab, constraint documented); (2) the OS-cfg gate greps raw file text including comments — a doc comment quoting the forbidden cfg_attr form tripped it (fixed: reworded, gate's exact grep reproduced locally before pushing).
- **F4 [accept]:** publication chain quality — the three library crates published cleanly after tarball reviews; the fresh out-of-tree probe resolved and compiled ktesio-engine 0.1.0 from crates.io; the crates.io names were Islam's own v0.0.1 placeholder reservations (2026-07-03, owners verified), reconciled in the runbook decision log.
- **F5 [fix-now, low]:** HOMEBREW_TAP_TOKEN no longer authenticates to iMagdy/homebrew-tap (created 2026-05-31; likely expired) — the tap update executed via the runbook's documented manual fallback (formula rendered locally from release checksums, pushed as 53dadf0, recorded in the decision log). **The secret needs renewal before the next tagged release.**
- **F6 [accept]:** story-level highlights that held: the 7-2 mutation-gap tests (append-failure silence, replay no-duplicate) survived the epic-level adversarial pass; the 7-3 audit teeth caught a second live stale-docs instance on first run; 7-5's measured budgets hold at 40-170× headroom.
- **F7 [record]:** the `ktesio` crates.io listing still shows the v0.5.0 "Agentic skills package manager" description until the next `cargo publish -p ktesio` — the v0.7.0 tag's workflow published the CLI but crates.io description text updates only on publish; it did publish 0.7.0, so the description is now current (verified in the tag workflow log: publish step ran).

## Behavior verification

- v0.7.0 release assets downloaded and checksums verified via the tap formula render (all four platform archives).
- Fresh out-of-tree probe (`/tmp/ktesio-host-probe`): `cargo add ktesio-engine@0.1.0` + build — compiled the full chain from crates.io (step 4 of the runbook).
- Docs site probes: / (200, current homepage), /adapter-contract (200), /embedding (200), /design/metering-agents-you-dont-control (200). /agents was 404 at session end — the registration fix (#175) is on main and lands with the next CF build.
- Local: 1143 tests green (epic tip), tarpaulin 95.30%, check_docs 26 files, test_automation 24/24.
- Not exercised: Windows binary run (built, not executed — no Windows runner interaction test); Homebrew install end-to-end (`brew install` not run — formula pushed, verification was URL-level).

## Previous-retro follow-through

Prior retro: epic-6 (2026-09-04). Its action items were executed in the same session: #160 (public-API inventory → superseded by the ARMED in-repo semver baselines for BOTH crates, landed in the epic-7 cycle's semver fix), #161 (memory.dir strip gap → still OPEN, owner next engine-touching story), #162 (frozen-doc corrections → landed in PR #180 era), #163 (TCK hardening → landed in the remediation), #165 (docs hygiene → landed), #166 (kt attach diagnostic → landed), #167 (docs current → landed), #168 (closed by ratification). The epic-6 retro's record is accurate; no drift found.

## Action items

1. **[ops] Renew HOMEBREW_TAP_TOKEN** (fine-grained PAT with Contents:read/write on iMagdy/homebrew-tap) before the next tagged release; the workflow's tap step fails on auth until then. Owner: Islam.
2. **[med] Live-docs probe:** add a scheduled check that the published docs site serves the newest registered page (the silent-CF-failure mode cost four pages for two weeks). Owner: next CI-touching story.
3. **[low] /agents landing check:** verify /agents serves 200 after the next CF build of main (fix merged, propagation pending at retro time). Owner: orchestrator.
4. **[record] Semver-baseline retire-or-keep decision** remains defaulted-KEEP until the second published release, per the runbook decision log. No action now.

## Acceptance verdict

**accepted** (criteria: **declared** — epics.md stories 7.1–7.5 ACs, with 7.4's publication completing post-merge on Islam's go). Every AC is met with evidence: the library-alone flow (7-1), the event bus with ordered schema-stamped payloads and the no-stall policy tested (7-2), embed-clean audits with mutation-verified teeth (7-3), the boundary proven and crates published (7-4 — the publication executed 2026-09-09/10 on the recorded go), and the NFR-4 budgets measured at 40–170× headroom (7-5). The publication hold was Islam's own instruction, honored and then lifted by his go — not an AC failure.

## Open questions

1. Should the docs deploy move INTO the repo's CI (a workflow that builds docs/ and deploys to Pages via the API), replacing the external platform dependency that silently rotted? (Pairs with action item 2.)
2. Is a Windows "smoke-run the release binary" job worth adding to the release workflow, given the binaries are built but never executed on Windows?
