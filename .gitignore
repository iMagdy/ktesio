/target
/debug/
/release/
*.rs.bk
*.rlib
*.prof*
.idea/
.vscode/
*.log
.env*
.DS_Store
Thumbs.db

# Python bytecode from the repo's gate scripts (scripts/*.py) — never tracked
__pycache__/
*.pyc

# BMAD Method tooling — the installed method itself stays local (it is a
# vendored toolchain, not this project's work product).
/_bmad/
/.agents/skills/bmad-*/
/.claude/skills/bmad-*/
/.github/agents/bmad-*
/.opencode/commands/bmad-*
